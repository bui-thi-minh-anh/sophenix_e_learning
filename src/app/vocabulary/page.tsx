import { PagePlaceholder } from "@/components/page-placeholder";

export default function VocabularyPage() {
  return <PagePlaceholder title="Từ vựng" description="Phần luyện từ vựng song ngữ sẽ hiển thị ở đây." />;
}
