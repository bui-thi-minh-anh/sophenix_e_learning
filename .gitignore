# dependencies
node_modules/
.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# TTS (OmniVoice) — môi trường Python + audio sinh ra (binary, không commit)
.venv-tts/
# Toàn bộ audio sinh bằng TTS (mọi module) — nặng, tái tạo được từ transcript.
#/public/audio/**/*.mp3
#/public/audio/**/*.wav
# Ảnh minh hoạ (illustration) được COMMIT vì thường ít & do người curate;
# nếu về sau sinh ảnh hàng loạt thì bỏ comment dòng dưới:
# /public/images/**/*.png
